# Backend configuration is handled from the root module. This file exists to satisfy
# the homework requirements and documents that the VPC module expects to use the
# same remote state backend as the root configuration.
