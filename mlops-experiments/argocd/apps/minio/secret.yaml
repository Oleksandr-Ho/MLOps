apiVersion: v1
kind: Secret
metadata:
  name: mlflow-minio-credentials
  namespace: mlflow
type: Opaque
stringData:
  MINIO_ROOT_USER: mlflow-access-key
  MINIO_ROOT_PASSWORD: mlflow-secret-key
  MINIO_DEFAULT_BUCKETS: mlflow-artifacts
