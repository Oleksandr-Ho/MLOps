apiVersion: v1
kind: Service
metadata:
  name: mlflow-tracking
  namespace: mlflow
  labels:
    app.kubernetes.io/name: mlflow-tracking
    app.kubernetes.io/component: tracking
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mlflow-tracking
    app.kubernetes.io/component: tracking
  ports:
    - name: http
      port: 5000
      targetPort: 5000
